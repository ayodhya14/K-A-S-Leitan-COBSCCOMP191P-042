class ContactAPI {
    static func getContacts() -> [Contact]{
        let contacts = [
            Contact(name: "Found 3 Corona Cases"),
            Contact(name: "Try To Keep Distance Always")
        ]
        return contacts
    }
}
